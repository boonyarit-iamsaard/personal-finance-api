spring:
  application:
    name: Finance
  datasource:
    url: jdbc:postgresql://localhost:5432/personal-finance
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true

security:
  jwt:
    # Generate a secure secret with: openssl rand -base64 32
    secret: ${JWT_SECRET:generate-secure-jwt-secret-with-openssl-rand-base64-32}
    expiration-ms: 900000 # 15 minutes in milliseconds

# Google OAuth2 configuration (for future implementation)
# Uncomment and configure when ready to implement Google OAuth
# spring:
#   security:
#     oauth2:
#       client:
#         registration:
#           google:
#             client-id: ${GOOGLE_CLIENT_ID}
#             client-secret: ${GOOGLE_CLIENT_SECRET}
